# forager — runtime state
data/
forager.db
forager.db-shm
forager.db-wal
*.db
*.db-shm
*.db-wal
tailscale-state/

# secrets (env layered atop config.json — never commit a real .env)
.env
.env.local
.env.*.local

# Go build artefacts
/forager
*.test
*.prof

# Plugin (Node/Vite)
plugin/node_modules/
plugin/dist/

# Personal deploy shortcut — keep your local copy at plugin/scripts/push.sh
plugin/scripts/push.sh

# Real-cache matcher corpus — built from the user's qBit + SAB history
# via `docker exec forager /build-corpus`. Contains real StashDB scene
# IDs and personal release-name history; never commit.
tools/matcher-bench/corpus.yaml
tools/matcher-bench/*.matcher.failures.csv

# Editor / OS
.DS_Store
*.swp
*.swo
.vscode/
.idea/
